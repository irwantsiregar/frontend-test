const LIMIT_LISTS = [
  { label: "10", value: 10 },
  { label: "20", value: 20 },
  { label: "50", value: 50 },
];

const LIMIT_DEFAULT = LIMIT_LISTS[0].value;

const PAGE_DEFAULT = 1;

const DELAY = 1000;

export { DELAY, LIMIT_DEFAULT, LIMIT_LISTS, PAGE_DEFAULT };
